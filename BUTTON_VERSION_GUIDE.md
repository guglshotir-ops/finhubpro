# 📌 Руководство по версионированию кнопки

## Как обновить версию кнопки

При каждом обновлении кода бота или мини-приложения нужно изменить версию кнопки, чтобы пользователи видели, что произошло обновление.

### Шаг 1: Найдите переменную версии

**Python:**
```python
BUTTON_VERSION = "v3.0"  # МЕНЯЙТЕ ЭТО ПРИ КАЖДОМ ОБНОВЛЕНИИ!
```

**Node.js:**
```javascript
const BUTTON_VERSION = "v3.0";  // МЕНЯЙТЕ ЭТО ПРИ КАЖДОМ ОБНОВЛЕНИИ!
```

### Шаг 2: Увеличьте версию

Примеры версий:
- `v3.0` → `v3.1` (минорное обновление)
- `v3.0` → `v4.0` (мажорное обновление)
- `v3.0` → `v3.0.1` (патч)

### Шаг 3: Перезапустите бота

После изменения версии перезапустите бота, чтобы изменения вступили в силу.

## Варианты отображения кнопки

В функции `get_button_text()` (Python) или `getButtonText()` (Node.js) можно выбрать формат:

### Вариант 1: С версией и датой
```python
return f"🚀 FinHub {BUTTON_VERSION} ({date_str})"
# Результат: 🚀 FinHub v3.0 (17.11)
```

### Вариант 2: С версией и временем
```python
return f"🚀 FinHub {BUTTON_VERSION} ({time_str})"
# Результат: 🚀 FinHub v3.0 (22:54)
```

### Вариант 3: Просто версия (рекомендуется)
```python
return f"🚀 FinHub {BUTTON_VERSION}"
# Результат: 🚀 FinHub v3.0
```

### Вариант 4: Без эмодзи
```python
return f"FinHub {BUTTON_VERSION}"
# Результат: FinHub v3.0
```

## Примеры версий

- `v1.0` - Первая версия
- `v1.1` - Небольшое обновление
- `v2.0` - Большое обновление
- `v2.1` - Исправления
- `v3.0` - Текущая версия с полноэкранным режимом

## Важно

- ✅ Меняйте версию при каждом обновлении
- ✅ Используйте семантическое версионирование (major.minor.patch)
- ✅ Перезапускайте бота после изменения версии
- ✅ Пользователи увидят новую версию кнопки сразу после перезапуска

